# 串口助手快捷指令使用指南

本文档详细介绍了如何在 Web 串口助手软件中添加、使用和管理快捷指令。快捷指令功能允许您保存常用的串口命令（如 AT 指令、Modbus 协议帧等），并实现一键发送，无需重复输入。

## 1. 功能位置

快捷指令面板位于**串口助手界面的右侧**。
在该面板中，您可以查看已保存的所有指令列表。

## 2. 添加快捷指令步骤

1.  **点击添加按钮**：
    在右侧“快捷指令”面板的标题栏右侧，点击 **➕ (加号)** 图标按钮。

2.  **填写指令信息**：
    弹出“添加快捷指令”对话框后，需填写以下两项：
    *   **指令名称**：给指令起一个易于识别的名字（例如："查询版本"、"开启LED"）。
    *   **指令内容**：输入实际要发送的命令字符串。

3.  **确认添加**：
    点击对话框底部的“确定”按钮。指令将立即显示在列表中。

## 3. 指令格式与发送模式

快捷指令保存的内容本质上是**“预填写的发送内容”**。点击快捷指令按钮时，软件会将该内容填入发送框并立即尝试发送。
因此，指令的生效方式取决于当前下方的 **发送设置**：

### 3.1 文本模式发送
如果**未勾选**“HEX发送”：
*   指令内容将被视为普通文本。
*   支持输入 AT 指令、JSON 字符串等。
*   **示例**：
    *   名称：`查询状态`
    *   内容：`AT+STATUS`
    *   *发送结果*：设备收到字符串 "AT+STATUS" (如果勾选了“发送新行”，会自动追加 `\r\n` 或 `\n`)。

### 3.2 十六进制 (HEX) 模式发送
如果**已勾选**“HEX发送”：
*   指令内容必须是合法的十六进制字符串（支持空格分隔）。
*   **示例**：
    *   名称：`开启继电器`
    *   内容：`A0 01 01 A2`
    *   *发送结果*：设备收到 4 个字节 `0xA0 0x01 0x01 0xA2`。

> **提示**：建议在指令名称中备注模式，例如 "LED开 (HEX)" 或 "Wifi连接 (Text)"，以免在错误模式下发送。

## 4. 常用指令示例

### 示例 1：ESP32 AT 指令
*   **名称**：`测试连接`
*   **内容**：`AT`
*   **发送设置**：文本模式，勾选“发送新行”

### 示例 2：查询设备信息 (JSON)
*   **名称**：`获取信息`
*   **内容**：`{"cmd": "info"}`
*   **发送设置**：文本模式

### 示例 3：Modbus 读保持寄存器
*   **名称**：`读温度数据`
*   **内容**：`01 03 00 00 00 02 C4 0B`
*   **发送设置**：**务必勾选 HEX发送**

## 5. 接收返回信息

**是的，可以收到返回信息。**

当您点击快捷指令发送命令后，设备返回的数据将显示在界面的 **“数据接收区”**（中间的大文本框）中。
*   发送的数据通常以绿色或特定标记显示（取决于设置）。
*   接收到的数据会实时追加在后面。

**注意**：快捷指令本身只是“快速填入并发送”，它不会单独弹窗显示结果，所有的交互流都在主窗口中统一展示。

## 6. 理解交互逻辑 (重要)

为了更好地使用本软件，由于串口通信通常遵循 **“查询 - 响应”** 模式，理解数据的流向非常有帮助：

*   **您 (用户/电脑端)**：是**主动方**。
    *   您在 **发送区** (或使用快捷指令) 发出命令。
    *   例如：发送 `查询温度`。
*   **设备 (ESP32/硬件)**：是**被动方**。
    *   设备收到命令后，执行操作，并**返回数据**。
    *   设备返回的数据会显示在 **接收区**。
    *   例如：接收区显示 `当前温度: 25°C`。

> **结论**：如果您想获得设备的信息，通常**必须先发送一条查询指令**，设备才会回复您。

## 7. 管理指令

*   **发送**：直接点击指令名称按钮。
*   **预览**：鼠标悬停在指令按钮上，会显示完整的指令内容提示框。
*   **删除**：点击指令按钮右侧的红色 **垃圾桶** 图标即可删除该指令。

## 8. 数据存储说明

快捷指令数据存储在您浏览器的 **LocalStorage**（本地缓存）中。
*   **优点**：无需登录，刷新页面不丢失。
*   **注意**：如果您清空浏览器缓存或更换浏览器/电脑，指令将会丢失。建议定期自行备份重要的指令内容。

## 9. 核心原理：设备端如何“回答”？

您问得很对！**发送仅仅是第一步，设备必须“听懂”并“回答”才算完成交互。**

根据您的使用场景，这取决于 ESP32 扮演的角色：

### 情况 A：ESP32 作为“透明传输网关” (默认场景)
*   **场景**：ESP32 的串口引脚 (TX/RX) 连接了另一个设备（如 STM32、温湿度传感器、继电器板）。
*   **逻辑**：
    1.  网页发送 `READ_TEMP`。
    2.  ESP32 收到后，原封不动地通过串口转发生给 **传感器**。
    3.  **传感器** 内部代码解析 `READ_TEMP`，并通过串口回复 `25.5`。
    4.  ESP32 收到串口数据，转发回网页。
*   **结论**：需要在**连接在 ESP32 后面的那个设备**里写好代码。

### 情况 B：ESP32 本身就是“被控设备”
*   **场景**：没有外接传感器，您直接控制 ESP32 上的 LED 或读取 ESP32 的状态。
*   **逻辑**：您需要修改 ESP32 的固件代码 (`.ino` 文件)，在接收回调函数中添加判断逻辑。

**代码示例 (Arduino/ESP32)**：
```cpp
// 在 firmware/esp32_serial_bridge/esp32_serial_bridge.ino 的 callback 函数中

void callback(char* topic, byte* payload, unsigned int length) {
  // ... (接收 msg 字符串) ...
  
  // ---在此处添加您的自定义逻辑---
  
  if (msg == "WHO_ARE_YOU") {
    // 收到询问，发送回复
    client.publish(data_topic.c_str(), "I am ESP32 Device #1");
  }
  
  else if (msg == "LED_ON") {
    digitalWrite(LED_BUILTIN, HIGH);
    client.publish(data_topic.c_str(), "OK: LED turned ON");
  }
  
  else if (msg == "READ_TEMP") {
    float t = readInternalTemp(); // 假如有这个函数
    client.publish(data_topic.c_str(), String(t).c_str());
  }
  
  // ---逻辑结束---
}
```

只有当您在设备端写了 `if` 判断并执行了 `publish` (或 `Serial.print`)，网页端才能收到“答案”。
